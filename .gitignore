# Los .sql son solo para correr en Supabase (uno tiene la contraseña). No van a la web.
*.sql

# Carpeta solo para Netlify Drop (Vercel sirve desde la raíz)
dist/

# Vercel / varios
.vercel/
node_modules/
.DS_Store
Thumbs.db
